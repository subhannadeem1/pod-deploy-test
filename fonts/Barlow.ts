import { Barlow as barlow } from 'next/font/google'

export const Barlow = barlow({ weight: '400', subsets: ['latin'], display: 'swap' })
export const BarlowBold = barlow({ weight: '900', subsets: ['latin'], display: 'swap' });