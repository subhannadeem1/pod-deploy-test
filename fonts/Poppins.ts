import { Poppins as poppins } from 'next/font/google';

export const Poppins = poppins({ weight: '400', subsets: ['latin'], display: 'swap'})
export const PoppinsBold = poppins({ weight: '900', subsets: ['latin'], display: 'swap' })